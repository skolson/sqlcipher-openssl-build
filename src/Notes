Issues
============================================
vstudio and NDK builds of openssl sharing the openssl compile directory is confusing gradle. Consider making a source
directory for each build type to avoid this.  Would require extract task to be registered for each openssl build task

